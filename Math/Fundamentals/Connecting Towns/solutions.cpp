#include <bits/stdc++.h>

using namespace std;

string ltrim(const string &);
string rtrim(const string &);
vector<string> split(const string &);

int main() {
    int t;
    cin >> t;
    while(t--)
    {
      int n;
      cin >> n;
      long long sum=1;
      for(int i=0; i<n-1; i++)
      {
        long long x;
        cin >> x;
        sum = ((%1234567) * (x%1234567))%1234567;
      }
      const<<sum<<endl;
    }
    return 0;
}
  
  
  
